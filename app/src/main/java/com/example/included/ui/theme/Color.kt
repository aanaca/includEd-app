package com.example.included.ui.theme

import androidx.compose.ui.graphics.Color

// Cores principais
val Blue = Color(135, 206, 235)      // Azul céu principal
val SkyBlueDark = Color(115, 186, 215)   // Tom mais escuro
val SkyBlueDeeper = Color(95, 166, 195)  // Tom ainda mais escuro

// Cores de superfície
val Surface = Color(255, 251, 254)       // Branco levemente rosado
val SurfaceDark = Color(28, 27, 31)      // Preto azulado

// Cores de texto
val OnSurfaceLight = Color(28, 27, 31)   // Preto azulado
val OnSurfaceDark = Color(230, 225, 229) // Cinza claro

// Cores de destaque
val White = Color(255, 255, 255)         // Branco puro